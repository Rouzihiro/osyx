# =============================================================================
# _variables.conf — personal variables
# Things that are mine. Override or extend variables.conf values here.
# =============================================================================

# Opens a URL as a Chrome PWA using the default profile
$webapp = google-chrome --profile-directory="Default" --app
