# config

This directory maps directly into `$HOME`.

Examples:

```text
config/.zshrc        -> ~/.zshrc
config/zsh/          -> ~/zsh/
config/.gitconfig    -> ~/.gitconfig
config/.config/hypr/ -> ~/.config/hypr/
```

Everything outside this directory is repo structure, package source, provisioning, docs, or assets.
