# Mako

Mako is the notification layer.

Theming confs: `~/.config/mako/config`

The generated config controls:

```text
background
text
borders
progress bars
urgency colors
opacity
spacing
icons
timeouts
```
